# 争议答辩卡（草案，2026-09-03 凌晨——若 9/10 进 Top 20 启用）

> 用途：线上答辩被追问争议点时的口径卡。原则：不背稿、只指证据；每个争议都落到「评委可 30 秒自验」的实物。

## 卡 1：公开仓 tag 的 README 写「五个学科」，实际是九个？

**一句话**：文字滞后，代码没缺。评委请直接跑 tag 里的判据脚本，别信 README 的文字。

**证据链**：
- tag `goai-round2-20260902` 内九个探针目录都有 `verify-*.mjs`，现场复跑 chem 8/8、gene 8/8
- 修正 commit `8e76e5b` 在 main 分支（只改 README 措辞，不改判据）
- 提交包 00-对齐表「已知问题披露」节已主动交代此事（不重打 tag 的理由也写了）

**如果评委追问「为什么不当场修 tag」**：tag 已被提交包与协作仓多处哈希引用，重打会让既有引用全部失效——用可核验的披露替代不可追溯的改写。

## 卡 2：11364 倍口径差——这是不是你们工具的 bug？

**一句话**：不是 bug，是本作品的主要发现——同一批 topic 在 OpenAlex 宽口径（全文宽松匹配）与精确标题短语口径下，计数倍数中位数 11364、85.7% 超 1000 倍。

**证据链**：
- `trend-probe/scale/result.json` → stats（266/1000 可测 topic，P1/P2 成立、P3/P4 被推翻——全部预注册，预测被自己数据推翻的原样入账）
- 预注册 `PREREG-v2.md` 哈希锚定在面板底部
- Crossref 交叉验证（中位数 1.94，P1/P2 不成立）如实写进负结果账——说明巨大差异不是跨库不变事实

**如果评委追问「所以结论是什么」**：结论是「AI 引用数据库说『没有』之前，必须先报告自己用的检索口径」——这正是作品标题那一步「可被检验」的含义。

## 卡 3：三条 exit 2（gap 13/16、sidon 6/8、sky 6/14）——没全过还敢叫判决器？

**一句话**：exit 2 是预注册判据真实未全过的诚实信号，不是故障；判决器连自己的未过都如实报告，这正是作品主张。

**证据链**：
- gap-probe 第 3 名预测 Fe-Ni-Cr（304 不锈钢基体，年产千万吨）——「数据库恰好 0 条」≠「该体系被研究过」，操作化定义缺口的证据就是这 3 条未过
- 面板三本账把未过三条与正结果并列展示，无软化词（check-panel.mjs P6 机器核验）
- 退出码语义写在面板「如何重新判决」节：0=全过、2=真实未过（有科学含义）、其他=环境故障

## 卡 4：这轮 Agent 是不是人扮演的？

**一句话**：不是。本轮 proposer.mjs 200 次自主探索，149 行完整 trajectory 日志在盘（logs/run-agent.log），130 条候选规律逐条带阈值与 p 值，未编辑。

**证据链**：
- `cd 代码 && node proposer.mjs --runs=200 | tee ../logs/run-agent.log` 可复跑
- 该争议源自更早版本状态（外部审计引述）；提交包 00 披露节第三条已交代

**备注（内部）**：opus 终审指出此条「反驳」混在「披露」清单里稀释诚实信号——答辩时如被问，按本卡口径回答即可，不必提审计。

## 卡 5：面板数字是手填的吗？

**一句话**：全部由 build-panel.mjs 生成时从结果 json 现读计算，重新跑一遍脚本就重新出数。

**证据链**：
- `node build-panel.mjs`（Node ≥18，离线）→ panel.html
- check-panel.mjs 机器核验：6/6 sha256 溯源命中真相源、九判决串齐全、无「趋势上支持/基本通过」类软化词
- 面板注明「重新生成即可重新判决」

## 使用规则（内部）

- 每卡先说一句话结论，评委点头就停，不展开
- 评委追问才给证据链第二层
- 所有哈希/命令当场可跑——说「我跑给您看」永远比「我保证」强
