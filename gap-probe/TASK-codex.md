# 交接给 codex — 当前状态与剩余工作

**Claude 侧到此为止（额度上限）。先读 `HANDOFF.md`（5 条红线 + 5 个实弹陷阱），再动手。**
最后一次提交：`0bccaca`。

---

## 一、已完成（别重做）

### 结论（两天两个数，端到端可核）

| 数 | 含义 | 出处 |
|---|---|---|
| **0 / 37** | 使用「数据库无条目 → 候选新材料」这一步的论文中，报告过该步骤假阴性率的篇数 | `litsearch-result.md`，45 个 DOI 全经 Crossref 核实 |
| **7 / 38 ≈ 18%** | 被声称「不存在」的化合物里，论文发表前已有已发表晶体结构的比例 | `result-table2.json` + `result-audit4.json` |
| **4 / 5** | 明确声明了「数据库缺席 ≠ 未合成」这条局限的论文数 | `check-npj2024-disclosure.md` + `audit4-source.json` |

### 最终命题（8/13 由 codex 的核查改写而成，比原设想更强）

> **披露不是控制。**
> 5 篇里 4 篇声明了局限，而**声明得最谨慎的那篇恰恰错得最多**（npj 2024，6/19 = 32%，最早隔 65 年）。
> 那篇用了 "some of them have not yet been synthesized"（非全称），
> 还用 Scopus / Google Scholar / LP / S 四路查过文献——**仍然漏了 6 个**。
> 散文里的限定语挡不住任何东西，只有会红的检查挡得住。

### 已核实、别再动的事实

- npj 2024 的限定语原话已由 Claude **独立回原文逐字核过**（`check-npj2024-disclosure.md`）
- 254/186/70% 的原始出处是 **Balachandran et al., PRM 2, 043802 (2018)**，不是 Bartel；
  真值 186/254 = 73.2%，口径「距凸包 ≤100 meV/atom」含亚稳相（`check-70pct.md`）
- 第二个领域实例（优先权主张审计）**已尝试并失败**，原因见 `HANDOFF.md` §4

### 锚点状态（2026-08-13 02:xx 已跑 upgrade）

```
21 个锚点：已进比特币区块 20　仅日历承诺 1　证明失效 0
```
**还有 1 个仅日历承诺**（是昨天新盖的两个之一）。**过 12~24 小时再跑一次**：
```bash
node governance/anchor.mjs upgrade
```

---

## 二、剩余工作

### 任务 C（主要，约 1~2 小时）：重做第二个领域实例

**目的**：让方法从「一个领域上的轶事」变成「方法」。现在只有材料学一个实例。

失败原因见 `HANDOFF.md` §4。三处必须修：

1. **学科过滤**——OpenAlex 的 `primary_topic` / `concepts` 限定到无机化学/材料，剔除生物医学。
   （旧版 200 条里 74 条核实通过，但绝大多数是结构生物学，而 COD 是无机晶体库。）
2. **化学式抽取器加黑名单**——要求至少两个真实元素符号，且不构成常见英文缩写。
   历史教训：`UP1` 是蛋白质结构域名，被读成 U(铀)+P(磷)，COD 返回 1941 年磷化铀。
3. **或**把蛋白质那一半改用 **PDB**（`data.rcsb.org`，免费，已验证可用）。

**这是新的一轮，不是打补丁**：
```bash
# 新建 PREREG-priority-v2.json（旧的 PREREG-priority.json 保留不动）
node governance/anchor.mjs build && node governance/anchor.mjs stamp
# 盖章之后，才允许发第一个查询
```

**成功判据**：拿到一个第二领域的率。**得到 0 也算成功**——只要覆盖率与作废条件如实报出。

### 任务 F（可选，先看清代价再决定）：把总体从「一个短语」扩到「九个短语」

现有结论的总体是 `"absent from the ICSD"` 这**一个**确切短语 → 7 条记录 = 5 篇。
`openalex-sweep.json` 里还有另外 8 个短语，合计 152 篇经摘要核实的论文，**未审**。

**先读这一段再决定要不要做——它大概率不该做：**

- `"not in the ICSD"` 检索计数 24,731，但结果里混着《International Classification of
  Sleep Disorders》——**ICSD 是同名词，也指国际睡眠障碍分类**
- `"not been synthesized"` 计数 259,884，结果里有 1959 年的《Tissue sulfhydryl groups》
- 而 `openalex-verify.mjs` 的回摘要核实**区分不了**「相关性退化」与「短语在正文里」：
  OpenAlex 检索全文、API 只吐摘要，所以核实率 0% 不代表搜错了
  （阳性对照就在那 7 条里——我们已审过的 npj 2024）

→ **仪器不足。硬跑就是拿噪音当分母。**
要做的话，先解决消歧（学科过滤 / 要求同时出现 ICSD 与化学语境词），
**并且必须新建 PREREG 重新盖章**，不能沿用现有那份。

**代价与收益都要摆出来再动手：不做也是一个正当选择，只要在文档里如实写明
「总体限于一个确切短语」——那已经写在 §1.3 里了。**

### 任务 E：文档定稿

草稿在 `DRAFT-问题定义文档.md`（约 5.3k 字符，四节 + 团队占位）。

- **第五节「团队介绍」codex 不要填**——成员背景、分工、过往成果只有本人知道
- 若任务 C 出结果，在 §1.3 与 §4.2 各加一段（第二实例的率 / 它失败过一次又重做）
- **文末「本文档自身的空缺」只剩 2 条，不许再删**：样本量是试点、Agent 自动化未实现。
  这两条是真的，删掉就变成我们自己在批的那种文档。

---

## 三、交付前必须跑（四条全绿才可提交）

```bash
node demo/gap-probe/verify-gap-probe.mjs   # 退出码 2 = 该轮作废，别提交结论
node governance/verify-naming.mjs          # 命名一致性 + 可理解性（含 N9/N10）
node governance/verify-anchor.mjs          # 含 A8.1 政策 ↔ 闸门双向对齐
node governance/publish.mjs --dry-run      # 密钥/隐私闸门，须 0 命中
```

提交前：`git status --porcelain | grep -i secret` **必须为空**。
**禁用 `git add -A`**（多会话并发，会替别人提交半成品）。

---

## 四、最后一句

这两天真正被验证的**不是那个 18%，是五次拦截**——
不锈钢、Hautier 2010、平凡解 77.6% 对 77.6%、AgGaSe₂、UP1。

**每一次，手上都有一个全绿的、算术正确的、看起来很轰动的结果。每一次它都是错的。
而拦下它的闸门，有三道是当天现造的。**

所以：**你接手之后产出的第一个漂亮结论，大概率也是错的。
先去找那道还没造出来的闸门，再去发表。**
